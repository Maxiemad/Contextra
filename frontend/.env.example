# Copy to .env.local for local overrides, or use .env.production for Render build.
# Backend URL (no trailing slash). Leave empty in dev to use the Vite proxy.
VITE_API_BASE_URL=https://contextra-b.onrender.com
# Optional: if the API requires X-API-Key (must match backend API_KEY)
# VITE_API_KEY=